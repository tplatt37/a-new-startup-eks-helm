apiVersion: v1
kind: Service
metadata:
  name: svc-a-new-startup
  namespace: "{{ .Values.namespace }}"
spec:
  type: NodePort
  selector:
    app: a-new-startup
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
