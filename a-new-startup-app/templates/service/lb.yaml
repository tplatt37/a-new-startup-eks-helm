apiVersion: v1
kind: Service
metadata:
  name: lb-a-new-startup
  namespace: mynamespace
spec:
  type: LoadBalancer
  selector:
    app: a-new-startup
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
